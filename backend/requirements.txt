# ── Django & REST (already installed or installing now) ───────────────
# Django 5.2.13 is already installed — use it as-is
# djangorestframework, django-cors-headers, python-dotenv need installing
djangorestframework==3.15.2
django-cors-headers==4.4.0
djangorestframework-simplejwt==5.5.1
django-filter==26.1
python-dotenv==1.0.1

# ── PostgreSQL driver ─────────────────────────────────────────────────
# psycopg v3 has Python 3.14 binary wheels
psycopg[binary]==3.2.9

# ── ML stack (already installed system-wide, listed for reference) ────
# scikit-learn==1.9.0    (installed)
# joblib==1.5.3          (installed)
# pandas==3.0.5          (installed)
# numpy==2.5.1           (installed)
# nltk==3.10.1           (installed)
